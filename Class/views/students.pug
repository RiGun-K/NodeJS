extends layout

block content
  input(type='text' id='studentId' placeholder='학생아이디')
  input(type='text' id='name' placeholder='이름')
  input(type='password' id='password' placeholder='비밀번호')
  p
  button(id='add') 추가
  p
  table(class="bluetop")
    thead
      tr 
        td 학생아이디
        td 이름
        td 삭제 
    tbody
      if students
        each s in students 
          tr 
            td=s.studentId  
            td=s.name 
            td  
              button(class='delete' id=s.studentId) 삭제