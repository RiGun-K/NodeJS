extends layout

block content
  input(type='text' id='teacherId' placeholder='선생아이디')
  input(type='text' id='name' placeholder='이름')
  input(type='password' id='password' placeholder='비밀번호')
  p
  button(id='add') 추가
  p
  table(class="bluetop")
    thead
      tr 
        td 선생아이디 
        td 이름 
        td 삭제 
    tbody
      if teachers
        each t in teachers 
          tr 
            td=t.teacherId  
            td=t.name 
            td  
              button(class='delete' id=t.teacherId) 삭제